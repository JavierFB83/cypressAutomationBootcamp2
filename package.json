{
  "name": "cypress_automation_bootcamp_2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:smoke": "cypress run --env tags=@smoke --spec '**/*.feature'",
    "cy:regression": "cypress run --env tags=@regression --spec '**/*.feature'",
    "cy:main": "cypress run --env tags=@main --spec '**/*.feature'",
    "cy:login": "cypress run --env tags=@login --spec '**/*.feature'",
    "cy:apiTest": "cypress run --spec '**/*.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^21.0.2",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.3",
    "axe-core": "^4.10.2",
    "cypress": "^13.15.1",
    "cypress-axe": "^1.5.0",
    "esbuild": "^0.24.0"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/e2e/step-definitions/*.js"
  },
  "dependencies": {
    "cypress_automation_bootcamp_2": "file:",
    "cypress-plugin-api": "^2.11.2"
  }
}
